"""Sample store parser that demonstrates expected HTML structure.

This is a sample parser to show how to add custom parsing logic for a store.
Real stores will need custom selectors or APIs configured in `config.yaml`.
"""

def adapt_items(html, selectors):
    # This module could implement custom parsing; we use the generic parser instead.
    # Provided for extensibility.
    return None
